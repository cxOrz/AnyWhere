# Copy this file to keystore.properties for a locally signed release build.
# Keep the real file and the keystore outside Git.
storeFile=/absolute/path/to/AnyWhere.jks
storePassword=your_keystore_password
keyAlias=your_key_alias
keyPassword=your_key_password
